// ContentView.swift — unused, replaced by PopoverView
// Kept to avoid file-not-found issues if referenced elsewhere.

import SwiftUI

struct ContentView: View {
    var body: some View {
        Text("Use the menu bar icon to interact with RadioMac.")
            .padding()
    }
}
